# =============================================================================
# DXTR Configuration
# =============================================================================

# --- LLM Providers ---
# Your OpenRouter API key (required if using OpenRouter models via LiteLLM)
OPENROUTER_API_KEY=sk-or-v1-...

# --- LiteLLM Proxy ---
# Master key for managing the LiteLLM instance (can be any random string)
LITELLM_MASTER_KEY=sk-...
# The API key used by the dxtr-core to authenticate with the LiteLLM proxy
LITELLM_API_KEY=sk-...

# --- Google Cloud Platform ---
# Your GCP Project ID (used for Docker image tags and GCS access)
GCLOUD_PROJECT_ID=your-project-id
# The GCS bucket name (e.g., 'dxtr-artifacts') for storing profiles and summaries
# Must be just the bucket name, not a gs:// URI
BLOB_STORE_URI=your-bucket-name

# --- Backend Security ---
# Static API Key for the DXTR FastAPI server. If set, requests must include
# 'Authorization: Bearer <key>' header. Leave empty for no authentication (local dev).
DXTR_API_KEY=your-secret-dxtr-api-key

# --- Database ---
# PostgreSQL connection string for papers database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- Observability (Production Only) ---
# Langfuse is used for production observability/tracing via LiteLLM's OTEL callback.
# Leave these unset for local development - LiteLLM will skip the langfuse callback.
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_OTEL_HOST=https://otel.langfuse.com